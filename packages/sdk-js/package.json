{
  "name": "@intent/sdk-js",
  "version": "0.1.0",
  "description": "Browser tracking SDK for the Intent Platform — drop-in like Segment/GA",
  "main": "dist/index.cjs.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "bundle": "tsc && rollup -c",
    "test": "jest"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0",
    "rollup": "^4.0.0",
    "jest": "^29.0.0"
  }
}
